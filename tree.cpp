struct Node {
  char data;
  Node *left;
  Node *right;
};

void Preorder(Node *root)
{
  if(root == NULL) return ;
  printf("%c", root->data);
  Preorder(root->left);
  Preorder(root->right)
}

void Preorder(F)
  void Preorder(D)
    void Preorder(B)
      void Preorder(A) NULL
      void Preorder(A) NULL
    void Preorder(C) NULL
    void Preorder(C) NULL
  void Preorder(E) NULL
  void Preorder(E) NULL
void Preorder(J)
  void Preorder(G)
    void Preorder(I)
void Preorder(K) 

void Inorder(Node *root)
{
  if(root == NULL) return;
  Inorder(root->left);
  printf("%c", root->data);
  Inorder(root->right);
}


void Postorder(Node *root)
{
  if(root == NULL) return;
  Postorder(root->left)
  Postorder(root->right)
  print
}

Postorder()
  Postorder()
    Postorder()
      Postorder()
        Postorder()
          Postorder()



